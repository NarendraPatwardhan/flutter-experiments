name: flutter_bazel_hello
description: Minimal Flutter app built with SpencerC rules_flutter (same UI as flutter-app).
publish_to: "none"
version: 1.0.0+1

environment:
  flutter: ">=3.24.0"
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
