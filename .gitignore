# Bazel
/bazel-*
.bazel-output/
MODULE.bazel.lock.bak
/execution.json
/bep.json
/bb-out/
/dist/
.bb-remote-invocation-id

# Build artifacts (download/stage locally — never commit)
*.tar.gz
linux_bundle.tar.gz

# Flutter / Dart local tooling (Bazel owns the hermetic SDK)
.dart_tool/
.packages
.pub-cache/
build/
.flutter-plugins
.flutter-plugins-dependencies

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
*~
